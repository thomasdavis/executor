{
  "name": "@assistant/bot",
  "private": true,
  "type": "module",
  "scripts": {
    "with-env": "bun run --env-file=../../../.env --",
    "dev": "bun with-env bun --hot src/index.ts",
    "start": "bun with-env bun src/index.ts"
  },
  "dependencies": {
    "@assistant/server": "workspace:*",
    "discord.js": "^14.25.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
